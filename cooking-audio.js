import {audioSettings} from './web-audio.js?v=20260909dev305';
import {cookingMotion} from './cooking-display.js';
export const COOKING_SOUNDS={mix:'mix',pepper:'pepper',boil:'boil',grill:'grill',cut:'chop-onion',shortCut:'chop-short',fry:'fry-pan',steak:'fry-steak',lightFry:'fry-pan-light'};
export function cookingSound(step,index=0){const [kind,text]=step||[];if(/후추.*뿌|향신료.*뿌/.test(text))return 'pepper';const motion=cookingMotion(step||[]);if(['mix','stir','knead'].includes(motion))return 'mix';if(['cut','grind'].includes(motion))return index%2?'chop-short':'chop-onion';if(['fry','saute'].includes(motion)||kind==='fry')return ['fry-pan','fry-steak','fry-pan-light'][index%3];if(motion==='boil')return 'boil';if(motion==='bake')return 'grill';return null}
let playing=null,key='';
export function stopCookingSound(){if(playing){playing.pause();playing.removeAttribute('src');playing.load()}playing=null;key=''}
export function playCookingSound(step,index,state){const settings=audioSettings(state),next=cookingSound(step,index),volume=settings.soundMuted?0:Math.max(0,Math.min(1,(Number.isFinite(Number(settings.soundEffectsVolume))?Number(settings.soundEffectsVolume):45)/100*(next==='boil'?1.6:1)));if(!next||!volume||document.hidden)return stopCookingSound();if(key===next&&playing){playing.volume=volume;return}stopCookingSound();key=next;playing=new Audio('./assets/audio/cooking/'+next+(next==='mix'?'.wav':'.mp3'));playing.volume=volume;playing.loop=true;playing.play().catch(()=>{});}
if(globalThis.document){document.addEventListener('visibilitychange',()=>{if(document.hidden)stopCookingSound()});window.addEventListener('pagehide',stopCookingSound)}
