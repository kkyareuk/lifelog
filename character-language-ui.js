import {LANGUAGE_FIELDS,languagePreview} from './character-language.js';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let serial=0;
export function characterLanguageFields(c,lang='ko'){
 const t=(ko,en,ja)=>({ko,en,ja}[lang]||ko),id='language-'+(++serial);
 const chips=(key,choices)=>`<div class="character-language-chips">${choices.map(([value,label])=>`<button type="button" data-language-pick="${key}" data-language-value="${esc(value)}" aria-pressed="${(c[key]||'')===value}">${esc(label)}</button>`).join('')}</div>`;
 const input=(key,label,choices=[],placeholder='')=>`<label>${esc(label)}<input data-field="${key}" value="${esc(c[key]||'')}" maxlength="40" autocomplete="off" spellcheck="false" placeholder="${esc(placeholder)}" ${choices.length?`list="${id}-${key}"`:''}>${choices.length?`<datalist id="${id}-${key}">${choices.map(v=>`<option value="${esc(v)}"></option>`).join('')}</datalist>`:''}</label>${choices.length?chips(key,[[ '',key.startsWith('self')?t('기본','Default','標準'):t('이름','Name','名前')],...choices.slice(0,4).map(v=>[v,v])]):''}`;
 const select=(key,label,options)=>`<label>${esc(label)}<select data-field="${key}">${options.map(([v,l])=>`<option value="${v}" ${v===(c[key]||'')?'selected':''}>${esc(l)}</option>`).join('')}</select></label>`;
 const own=t('자신을 부르는 말','First-person expression','自分の呼び方'),reference=t('다른 사람이 부르는 방식','Third-person reference','第三者からの呼び方'),auto=t('비우면 말투에 맞춤','Leave empty to follow the voice','空欄なら口調に合わせる'),name=t('비우면 이름 사용','Leave empty to use the name','空欄なら名前を使う');
 const form=code=>{
  let html='';if(code==='ko')html=input('selfKo',own,['나','저','짐','본인','이 몸'],auto)+input('referenceKo',reference,['그','그녀'],name)+`<details><summary>${t('문장 형태 조정','Adjust word forms','語形を調整')}</summary>${input('selfKoSubject',t('주어 형태 · 예: 내가','Subject form · e.g. 내가','主語形・例：내가'),[],t('비우면 자동','Automatic if empty','空欄なら自動'))}${input('selfKoPossessive',t('소유 형태 · 예: 내','Possessive form · e.g. 내','所有形・例：내'),[],t('비우면 자동','Automatic if empty','空欄なら自動'))}</details>`;
  if(code==='ja')html=input('selfJa',own,['私','僕','俺','わたくし','自分','余'],auto)+input('referenceJa',reference,['彼','彼女'],name);
  if(code==='en')html=select('pronounEn',t('영어 대명사','English pronouns','英語の代名詞'),[['',t('이름 사용','Use name','名前を使う')],['he','he / him'],['she','she / her'],['they','they / them'],['custom',t('직접 입력','Custom','自由入力')]])+`<div data-language-custom ${c.pronounEn==='custom'?'':'hidden'}>${input('pronounEnSubject',t('주격 · ze','Subject · ze','主格・ze'))}${input('pronounEnObject',t('목적격 · hir','Object · hir','目的格・hir'))}${input('pronounEnDeterminer',t('소유 한정사 · hir','Possessive determiner · hir','所有限定詞・hir'))}${input('pronounEnPossessive',t('소유 대명사 · hirs','Possessive pronoun · hirs','所有代名詞・hirs'))}${select('pronounEnAgreement',t('동사 형태','Verb agreement','動詞の形'),[['', 'is / has / does'],['plural','are / have / do']])}<small>${t('다섯 형태를 모두 입력하기 전에는 이름을 사용해요.','Names are used until all five forms are filled in.','5つの語形が揃うまでは名前を使います。')}</small></div><div data-language-reflexive ${['custom','they'].includes(c.pronounEn)?'':'hidden'}>${input('pronounEnReflexive',t('재귀형','Reflexive','再帰形'),['themselves','themself'],c.pronounEn==='they'?'themselves':'hirself')}</div>`;
  return `<div class="character-language-fields" data-language-code="${code}">${html}<div class="character-language-sample"><small>${t("이렇게 표현돼요","A little preview","こんな表現になります")}</small><p class="character-language-preview" data-language-preview="${code}">${esc(languagePreview(c,code))}</p></div></div>`;
 };
 return `<div class="character-language-control"><button type="button" data-open-language-settings aria-haspopup="dialog">${t("대명사 설정","Pronoun settings","代名詞の設定")} <span aria-hidden="true">›</span></button><dialog class="character-language-dialog" aria-label="${t("대명사 설정","Pronoun settings","代名詞の設定")}"><header><strong>${t("대명사 설정","Pronoun settings","代名詞の設定")}</strong><button type="button" data-close-language-settings>${t("완료","Done","完了")}</button></header><fieldset class="character-language-settings"><legend>${t('대명사·자칭','Pronouns & self-reference','代名詞・一人称')}</legend><div class="character-language-heading"><span class="character-language-seal" aria-hidden="true">✎</span><p>${t('캐릭터만의 부르는 말','In their own words','その人らしい呼び方')}<small>${t('성별과 별개로, 비워 두어도 괜찮아요.','Independent of gender. Leaving this empty is fine.','性別とは別の設定。空欄でも大丈夫です。')}</small></p></div>${form(lang)}<details class="character-language-other"><summary>${t('다른 언어에서의 표현','Expressions in other languages','ほかの言語での表現')}</summary>${['ko','en','ja'].filter(l=>l!==lang).map(l=>`<details class="character-language-locale"><summary>${({ko:'한국어',en:'English',ja:'日本語'})[l]}</summary>${form(l)}</details>`).join('')}</details></fieldset></dialog></div>`;
}
export function bindCharacterLanguageFields(root,character){
 root.querySelectorAll('.character-language-control').forEach(control=>{
  if(control.dataset.bound)return;control.dataset.bound='true';const dialog=control.querySelector('dialog'),opener=control.querySelector('[data-open-language-settings]');opener.onclick=()=>dialog.showModal();control.querySelector('[data-close-language-settings]').onclick=()=>dialog.close();dialog.addEventListener('close',()=>opener.focus());
 });
 root.querySelectorAll('.character-language-settings').forEach(box=>{
  if(box.dataset.bound)return;box.dataset.bound='true';
  const refresh=()=>{const draft={...character};box.querySelectorAll('[data-field]').forEach(el=>{if(LANGUAGE_FIELDS.includes(el.dataset.field))draft[el.dataset.field]=el.value});box.querySelector('[data-language-custom]').hidden=draft.pronounEn!=='custom';box.querySelector('[data-language-reflexive]').hidden=!['custom','they'].includes(draft.pronounEn);box.querySelectorAll('[data-language-pick]').forEach(el=>el.setAttribute('aria-pressed',String((draft[el.dataset.languagePick]||'')===el.dataset.languageValue)));box.querySelectorAll('[data-language-preview]').forEach(el=>el.textContent=languagePreview(draft,el.dataset.languagePreview));};
  box.addEventListener('click',event=>{const button=event.target.closest('[data-language-pick]');if(!button)return;const input=box.querySelector('[data-field="'+button.dataset.languagePick+'"]');input.value=button.dataset.languageValue;input.dispatchEvent(new Event('input',{bubbles:true}));input.dispatchEvent(new Event('change',{bubbles:true}));});
  box.addEventListener('input',refresh);box.addEventListener('change',refresh);refresh();
 });
}
