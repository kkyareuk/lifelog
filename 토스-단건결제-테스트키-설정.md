# 토스페이먼츠 단건결제 테스트키 설정

확인된 단건결제 MID는 `drawerg8ht`입니다. 이 프로젝트는 결제창 SDK를 사용하므로 토스 개발자센터에서 **API 개별 연동 키**의 테스트 키 세트를 사용합니다.

## 1. 브라우저용 테스트 클라이언트 키

1. 토스페이먼츠 개발자센터에서 MID `drawerg8ht`를 선택합니다.
2. API 개별 연동 키의 `test_ck_`로 시작하는 **테스트 클라이언트 키**를 복사합니다.
3. `config.js`의 아래 한 줄에서 따옴표 사이에 붙여 넣습니다.

```js
const tossPaymentsClientKey="test_ck_여기에_복사";
```

클라이언트 키는 결제창을 여는 공개 키라 웹 코드에 들어갈 수 있습니다. `test_sk_` 또는 `live_sk_` 키는 절대 이 파일에 넣지 않습니다.

## 2. 서버용 테스트 시크릿 키

PowerShell에서 이 프로젝트 폴더를 연 뒤 아래 명령을 실행합니다.

```powershell
npx.cmd firebase-tools functions:secrets:set TOSS_SECRET_KEY --project lifelog-98fff
```

입력하라는 문구가 나오면, 같은 API 개별 연동 키 세트의 `test_sk_`로 시작하는 **테스트 시크릿 키**를 붙여 넣습니다. 시크릿 키는 화면에 표시되지 않는 것이 정상입니다.

그다음 결제 승인 서버를 배포합니다.

```powershell
npx.cmd firebase-tools deploy --project lifelog-98fff --only functions:api
```

Cloud Functions 배포에는 Firebase 프로젝트의 Blaze 요금제와 해당 계정의 배포 권한이 필요합니다. `403 Caller does not have required permission`이 다시 나오면 `lifelog-98fff` 프로젝트 소유자 계정으로 로그인했는지와 IAM의 Service Usage 권한을 먼저 확인해야 합니다.

## 3. 테스트 순서

1. 변경한 사이트 파일을 GitHub에 올립니다.
2. 사이트에서 Google 로그인합니다.
3. 상점에서 상품을 장바구니에 담고 `장바구니 결제하기`를 누릅니다.
4. `토스로 ...원 결제하기` 버튼을 눌러 테스트 결제창을 엽니다.
5. 테스트 환경에서는 실제 카드 정보를 입력해도 실제 금액은 빠져나가지 않습니다.
6. 성공 화면까지 확인하고 토스 담당자에게 결제 경로를 회신합니다.

## 4. 라이브 전환 전 주의

- 카드사 심사 전에는 `test_ck_` + `test_sk_` 세트만 사용합니다.
- 라이브 키는 계약·심사가 끝난 뒤 같은 방식으로 교체합니다.
- 클라이언트 키와 시크릿 키는 반드시 같은 세트여야 합니다.
- Android 앱의 디지털 상품은 토스가 아니라 Google Play 결제를 유지합니다.
