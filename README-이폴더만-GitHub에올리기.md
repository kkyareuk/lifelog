# 이 폴더의 내용만 GitHub 저장소 루트에 올리세요

폴더 자체를 올리는 것이 아니라, 이 폴더 **안의 파일과 `functions` 폴더**를 GitHub 저장소의 같은 위치에 덮어씁니다.

## 올리기 전에 한 번만 할 일

`config.js`를 열고 MID `drawerg8ht`의 API 개별 연동 키 중 `test_ck_`로 시작하는 테스트 클라이언트 키를 아래 줄의 따옴표 안에 붙여 넣습니다.

```js
const tossPaymentsClientKey="";
```

`test_sk_`로 시작하는 테스트 시크릿 키는 `config.js`나 GitHub에 넣지 않습니다. 자세한 PowerShell 명령은 `토스-단건결제-테스트키-설정.md`에 적어 두었습니다.

## 포함된 변경

- 사업장 주소를 사업자등록증 표기와 동일하게 수정
- 토스 단건결제 주문 생성·금액 검증·승인 서버 추가
- 테스트 클라이언트 키와 테스트 시크릿 키 분리
- 관계의 두려움을 이유가 아닌 강도 단계로 변경
- 과거 두려움 설정값 자동 변환
- 웹/앱 캐시 버전 `20260810p`
