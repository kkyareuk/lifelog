# dev325 / 1.0.292

簡単キャラクタープロフィールを旧 quick modal に復元。profile photo / ldImage upload buttons reuse existing image picker; shared placement dialog uses existing characterHomeLayoutEditor. Personal full-settings 2-page profile route removed from entry. Family expansion relation button and shop card hidden, existing family data/types preserved.

Locks: each supported field title contains small icon and localized Locked/Unlocked label; dark brown locked/light olive unlocked. Global book/section locks removed. All25 personality/emotion fields support manual locks. Shared owner editor locks save with existing saveResident; failed save restores metadata and reports retry. Existing v1 discovery profiles protect the18 newly supported fields until explicitly unlocked; v2 new characters allow discovery across25. Legacy imported characters remain protected.

Questions: 24 separate situations, exactly5 actions each (120), all KO/EN/JA. Independent hypothetical unexpected scenes, not domain questionnaires. Each action has several hidden effects. Ordered axes add +/-1 to bounded0..100 scores; categorical reactions accumulate per-option evidence, never assume recovery methods or feelings form an ordered scale. Manual edits reset that field evidence and lock it. Protected fields skipped even when an action changes other fields. 25 fields covered by event effects. Recent6 answered situations excluded; 3..8minute session interval/3minute per-character delay, random gate, first scene baseline, no backlog. Current questions are hypothetical and do not teleport actors or invent actual room occupancy. Existing log generation reads resulting character settings; continuous observation reads scores where supported. Existing past logs remain preserved.

Multiplayer: render obtains active resident/current scene from isolated shared world, passes group context. Only resident.ownerUid=current account eligible. Response re-reads latest snapshot, rechecks current account/group/ownership and locks; calls existing saveResident with latest profile + patch. Auth saveResident owns media/session checks. No server API changes or deployments in this update. Existing server save-resident transaction checks member+owner and clears group lifeUpdatedAt for simulation recompute. Shared failures do not mutate local personal characters. Cross-device simultaneous full-profile edits still follow the existing saveResident conflict semantics; no new transaction-level per-field merge was introduced.

Verification: pure25-field/24-event/120-choice/3-language coverage, boundedweights/multi-axis/locks/no-backlog. Real Chrome KO/EN/JA x384/1180: quick modal/media buttons/placement dialog, title locks, five choices with no weights rendered, choice persistence, hidden family entry, mocked shared owner/group/save failure. Local transaction fake against actual functions/save-resident: own profile score metadata persisted, future simulation invalidated, other owner/nonmember forbidden. Existing log continuity check passed. No real two-account server roundtrip or Android/iOS device QA; no signed IPA. Android APK/AAB signed and all303 web assets checked identical. Direct files at root 앱 전달. New questions/actions all3languages; legacy static coverage EN2236/2945(75.9%), JA2235/2945(75.9%), direct3language module strings not counted by that legacy scanner.

Deployment: app dev / board main only. Production app main untouched. No Play/production web/server deployment, no mail or push.
