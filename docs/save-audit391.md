# 391 저장 안정성 점검 — 2026-09-14

기준: dev 082487e, Android 1.0.339 / versionCode 391. 앱 실행 코드 변경 없음. 테스트와 기록만 변경하므로 신규 빌드·업로드·버전 증가는 하지 않음.

## 결과
- 신고된 noise 질문: 모든 8개 선택지를 성향 고정 상태에서 답변하고 매번 다시 불러오기. 답변 이력 저장, 고정 성향 유지. Chromium/WebKit 통과.
- 캐릭터 5명 각각 다른 욕구5종 값과 관계 정책을 저장하고 다시 불러오기. 값·고정 설정·관계 정책 모두 유지.
- 질문 임시 기록 저장 실패를 주입: 실패 전달, 답변 변경 롤백. 정상화 후 재시도 가능.
- 전체 저장 실패를 주입: false 반환, 이전 영구 저장본과 현재 화면의 수정 내용 보존. 저장 재시도 후 재실행 복원.
- 자동 저장 대기 중 pagehide: 종료 직전 변경이 실제 저장소에 즉시 기록되는지 확인.
- 저장 도중 새 수정, 계정 전환/왕복, 삭제: 오래된 비동기 결과가 최신 저장이나 다른 계정을 덮지 않음.
- 저장 한도: 압축 및 IndexedDB 이동, 복구본 보존, 실패한 교체 롤백, 계정 격리, 손상 복구본 보존 통과.
- 관계 수치·관계 우편·욕구 고정 UI 저장 및 다시 불러오기 통과.
- 명시 저장 실패 안내: 한국어/영어/일본어 모두 실패를 성공으로 처리하지 않음.

## 검사 보완
- check-save-read311: 현재 명시 저장 함수가 사용하는 saveFailureMessage를 검사 실행 환경에 주입.
- check-restore-storage: 현재 기본 데이터가 증가해 낡은 30KB 성공 시나리오 예산을 초과함. 성공 복구 시나리오 예산을 200KB로 변경. 앞부분의 엄격한 용량 제한/압축/보존 검사는 유지.
- 신규 qa-save391: 실제 브라우저와 실제 상태/질문 저장 모듈을 사용하는 실패 주입 및 재실행 검사. 초기 기본값/잠금 메타데이터 정규화 후 기준을 잡아 답변 변화만 비교.

## 실행한 검사
check-input-save361, check-save-read311, check-save-pricing-dev, check-restore-storage, check-account-isolation, check-cloud-character-merge, check-forward-schema-recovery, check-answer-journal345, check-snapshot370, qa-snapshot385(Chromium), qa-mail390(Chromium/WebKit), qa-life388(WebKit), qa-save391(Chromium/WebKit).

## 확인 범위와 한계
이 범위에서 추가 앱 결함을 발견하지 못했다. 모든 기기와 모든 실패 상황이 해결됐다는 보증은 아니다. 테스트용 데이터만 사용했고 외부 네트워크를 차단했다. 실제 신고 계정 데이터, Android 실기기 강제 종료/저장장치 장애, 운영 Firebase의 인증된 저장 왕복은 이번 검사에 포함하지 않았다. 클라우드 병합·계정 경계는 모의 응답으로 검증했다. pagehide는 브라우저 생명주기 이벤트 검사이며 OS 강제 종료와 동일하지 않다.

앱/사이트/iOS 공통 실행 코드와 번역 문구는 변경 없음. 기존 391 변경 범위 영어·일본어 100% 유지(앱 전체 번역률을 의미하지 않음).
