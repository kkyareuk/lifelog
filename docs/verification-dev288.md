# 개발·검증 기록: 288 / 1.0.255

- 전송 알림 기존 구조/스타일 수정. 중복 실행 방지, 실패 시 수동 재시도, 닫기 후 진행 중 저장 유지.
- 그룹 대표 사진은 기존 cropImage 정사각형 편집기를 사용. 취소 시 업로드 없음, 500×500 WebP로 업로드. 편집 중 그룹 변경 시 잘못된 그룹에 저장하지 않음.
- 그룹 카드 기존 CSS 수정: 2열, 정사각형 사진, 통계 3칸, 추가 카드 정렬의 특이도 충돌 수정.
- 답변 우편 responderPhoto/이름을 서버에서 제공하고 프런트에서 발신 방향 분리. 원본 proposal 객체를 변경하지 않음. 기존 제안 표에 관계 종류/단계 표시 유지.
- 집 안 명령 대화 말풍선/고개 움직임은 CSS로만 표시. 끄기·효과 줄이기와 시스템 reduced-motion 존중.
- 대화 주제에 사람/사전 항목 추가. 뒷담화는 도착한 활성 명령, 같은 집·방 또는 같은 지정 장소일 때만 현재 장면에 반영. 잠든 인물 제외. 반응은 상태 쓰기 없는 파생 표시이며 별도 영구 기록을 추가하지 않음.
- qa-mail288: 폰/태블릿 기존 편집·우편·백그라운드 저장 회귀, 그룹 카드 정렬/카운터, 사진 취소·적용 업로드, 답변 사진·보내는 이/받는 이·원본 객체 유지 통과.
- qa-command288: 격리 두 계정 이동/대화/집 애니메이션, 터치 스크롤 및 기존 편집 회귀 검사. 실제 Android 2대 네트워크 검증을 대체하지 않음.
- check-gossip288: 같은 장소·도착·수면·만료 조건, 성격별 반응, KO/EN/JA, 상태 무변경 통과. check-service287 권한/전송/중복 생활 계산 검사 통과.
- Android release AAB/APK 빌드·서명 검증, 257개 자산 바이트 일치, APK 추출 자산 오프라인 시작·저장 후 재실행 통과.
- sharedTownApi 배포 성공. 이동 프레임별 좌표 저장이나 추가 주기 조회 없음. 실제 청구액·기기 발열·느린 모바일 네트워크 업로드 지연은 미측정.
- 공통 웹 소스에 반영. 공개 웹 배포, 운영 게임 main, 기존 Apple 제출 빌드는 변경하지 않음. 새 IPA 생성·App Store 업로드 없음.
- 정적 번역률 EN 2216/2912(76.1%), JA 2215/2912(76.1%). 신규 주요 문구와 출시노트 3개 언어 포함.
