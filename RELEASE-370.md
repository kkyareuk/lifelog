# Android 1.0.328 (370) · 저장 구조 분리

## 실기기 확인
- USB 연결 성공. 설치본 368 / 1.0.328, 업데이트 19:48 확인. APK 내부 청크 내보내기 모듈 존재 확인.
- 사용자가 20:15 백업 저장 완료. USB로 8,007,608바이트 JSON 수집·파싱 성공. 비공개 진단 파일이며 커밋하지 않음.
- 보존된 서랍마을 Java 크래시는 업데이트 전 19:17·19:20 TransactionTooLargeException. 이번 내보내기 후 같은 프로세스 유지. 모든 크래시 해소를 보장하는 결과는 아님.

## 구조
- snapshot-codec: 기존 gzip 형식과 검증을 독립 모듈로 유지. 이전 저장 파일 호환.
- snapshot-worker/client: 압축·압축 해제 검증·용량 부족 재압축을 Worker에서 수행. 오류/시간초과는 저장 실패로 전달, 재시도 가능.
- account-storage: async 기록 API와 키별 revision 및 계정 전환 epoch. 작업 중 최신 저장·삭제·계정 전환 발생 시 오래된 결과 폐기. 기존 다른 계정 데이터 삭제 없음.
- state: 하나의 저장 작업과 최신 대기 요청으로 합침. 완료된 압축 결과를 복구 사본에도 재사용. UI save(true)는 즉시 비동기 저장을 시작하며 완료 Promise 반환; 완료를 확인하는 캐릭터/사전 저장 호출은 await 처리. 종료와 계정 전환은 동기 flush를 유지.
- JSON 직렬화, 최종 localStorage 기록, 복구 후보 읽기는 아직 메인 스레드 작업. 이번 변경이 모든 렌더 지연을 제거한다고 주장하지 않음.

## 검증
실제 백업 데이터를 사용한 PC 비교(이름/사진 등은 출력하지 않음):
- Chromium CPU4x: 동기 압축 경로 최대 타이머 간격 1,522~1,592ms, Worker 경로 84~129ms.
- WebKit: 211~212ms → 17~28ms. 총 처리시간과 화면 멈춤 시간은 구분. 실제 폰 측정값 아님.
- 저장 원문 동일성, quota 복구, 최신 쓰기 우선, 계정 왕복, 삭제, 실패시 원본 보존, Worker 실패 재시도 통과.
- 브라우저 실제 state API에서 최신 수정, 종료 flush, 초기화 후 재등장 방지 통과.
- 계정 격리, 입력 중 저장 대기, 대용량 내보내기 회귀 통과.
- APK/AAB 빌드, APK 서명·370 버전, APK 내 모듈과 원본 일치 확인.

369의 가구·건강 기록 수정 포함. 새 사용자 문구 없음: 기존 KO/EN/JA 문구 사용, 369 신규 번역 범위 EN/JA100%, 전체 미산정. 스토어 미배포, iOS 심사 미제출. 실제 휴대폰에 370 설치 후 반복 질문·화면 이동 검증 필요.
