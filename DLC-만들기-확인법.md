# 서랍마을 DLC 만들기와 확인

## 현재 DLC가 적용되는 구조

- 계정이 가진 DLC ID는 Firebase 사용자 문서의 `entitlements.dlcPacks` 배열에 저장됩니다.
- 화면에서는 `hasDlc("DLC_ID")`로 소유 여부를 확인합니다.
- 현재 예시는 `medieval`이며, 중세 시대와 중세 건물 모양을 선택할 때 이 권한을 검사합니다.
- 결제 성공 뒤 서버가 사용자 문서에 DLC ID를 넣어 주어야 실제 구매 계정에만 적용됩니다.

## 새 DLC를 만드는 순서

1. 겹치지 않는 ID를 정합니다. 예: `military_life`, `noir_city`.
2. `views.js`의 상점 상품 목록에 이름, 가격, 설명을 추가합니다.
3. DLC 전용 이미지와 건물 파일을 별도 폴더에 넣습니다.
4. 전용 선택지를 표시하거나 사용할 때 `hasDlc("DLC_ID")`를 검사합니다.
5. 전용 생활 장면은 기본 장면과 섞지 말고 DLC ID별 배열로 분리합니다.
6. 결제 승인 서버의 상품표에 같은 ID와 정확한 금액을 등록합니다.
7. 승인된 결제에만 해당 ID를 `entitlements.dlcPacks`에 기록합니다.

## 결제 없이 개발 중 확인하는 방법

Firebase Console에서 본인 테스트 계정의 사용자 문서에 다음처럼 시험용 권한을 넣습니다.

```json
{
  "entitlements": {
    "dlcPacks": ["medieval", "새_DLC_ID"]
  }
}
```

로그아웃 후 다시 로그인하거나 설정에서 데이터를 다시 불러온 다음 아래를 확인합니다.

- 테스트 계정에서는 DLC 선택지가 활성화되는가
- 다른 테스트 계정에서는 잠겨 있는가
- 새로고침 뒤에도 권한이 유지되는가
- 다른 계정으로 DLC 데이터가 섞이지 않는가
- DLC를 제거했을 때 기존 저장 데이터가 깨지지 않고 기본 콘텐츠로 대체되는가

## 공개 전 필수 검사

- 클라이언트가 가격이나 DLC ID를 보내더라도 서버가 등록된 상품표로 다시 검증해야 합니다.
- 클라이언트에서 `dlcPacks`를 직접 수정해도 Firebase 보안 규칙이 저장을 거부해야 합니다.
- 결제 성공 페이지를 주소로 직접 열어도 DLC가 지급되면 안 됩니다.
- 같은 주문번호를 두 번 보내도 한 번만 지급되어야 합니다.
- 환불 시 권한 회수와 이미 만든 DLC 건물·저장 데이터 처리 방침을 정해야 합니다.

