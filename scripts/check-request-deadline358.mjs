import assert from 'node:assert/strict';import {requestDeadline} from '../request-deadline.js';assert.equal(await requestDeadline(Promise.resolve(42),'ok',10),42);const err=Error('offline');await assert.rejects(requestDeadline(Promise.reject(err),'error',10),e=>e===err);let complete;let applied=false;const pending=new Promise(r=>complete=r);await assert.rejects(requestDeadline(pending,'account-save',5).then(()=>applied=true),e=>e.code==='sync/request-timeout');complete(42);await Promise.resolve();assert.equal(applied,false);console.log('PASS immediate success, original error, hung read deadline, late read cannot apply timed-out continuation');
